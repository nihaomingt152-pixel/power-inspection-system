# 电力输电线路智能巡检平台 使用手册

> **系统名称**：电力输电线路智能巡检平台 v3.0
> **副标题**：基于 YOLO + 多模态 AI 的输电线路智能检测分析预警系统
> **版本号**：v3.0（Phase 0 - Phase 30）
> **生成日期**：2026 年 8 月 6 日
>
> **适用对象**：项目评审专家、技术评委、电力行业管理者、系统运维与检修人员

---

## 目录

- [一、系统概述](#一系统概述)
  - [1.1 行业背景](#11-行业背景)
  - [1.2 解决方案](#12-解决方案)
  - [1.3 核心价值](#13-核心价值)
- [二、系统核心功能详解](#二系统核心功能详解)
  - [2.1 图片检测](#21-图片检测)
  - [2.2 视频分析](#22-视频分析)
  - [2.3 实时摄像头检测](#23-实时摄像头检测)
  - [2.4 多模态 AI 分析（功能开关）](#24-多模态-ai-分析功能开关)
  - [2.5 兜底异物检测（功能开关）](#25-兜底异物检测功能开关)
  - [2.6 工单闭环管理](#26-工单闭环管理)
  - [2.7 数据仪表盘](#27-数据仪表盘)
  - [2.8 GIS 地图总览](#28-gis-地图总览)
  - [2.9 移动端访问优化](#29-移动端访问优化)
- [三、完整演示案例（评委讲解脚本）](#三完整演示案例评委讲解脚本)
- [四、系统架构简图](#四系统架构简图)
- [五、常见问题 FAQ](#五常见问题-faq)

---

## 一、系统概述

### 1.1 行业背景

传统输电线路巡检依赖人工定期登塔巡视，存在三大痛点：

| 痛点 | 具体表现 | 后果 |
|------|----------|------|
| **人力成本高** | 巡检人员需携带专业设备现场作业，单塔巡视耗时耗力 | 巡检频次受限，覆盖率低 |
| **漏检率高** | 绝缘子破损、闪燃损坏等微小缺陷依赖肉眼分辨，易疲劳误判 | 缺陷发现滞后，隐患扩大 |
| **缺乏闭环管理** | 缺陷记录停留在纸质台账，从"发现"到"修复"无流程追踪 | 修复责任不清，闭环周期长达数天 |

### 1.2 解决方案

本系统以 **YOLO 目标检测 + Qwen3-VL 多模态大模型 + 工单闭环管理** 为核心，构建"**发现 → 研判 → 派单 → 修复 → 闭环**"的一体化智能巡检链路：

- **AI 识别**：YOLO 实时识别 8 类线路目标，秒级定位缺陷；
- **大模型研判**：Qwen3-VL-Flash 对缺陷进行语义级深度分析，输出成因推断与维修建议；视频分析以**整段视频一次性总结**方式生成结构化报告（全程仅调用 1 次 API）；
- **闭环管理**：工单全流程状态流转，从派发、处理、复检到闭环全程可追溯。

### 1.3 核心价值

> **让巡检从"人眼看"升级为"AI 识 + 大模型判 + 闭环管"。**

- 单张图片检测秒级完成，从发现到派发仅需 **30 秒**；
- 视频分析通过**智能跳帧 + GPU 批量推理 + 硬件编码**大幅提速，且**整段视频只调用 1 次 AI**，成本较逐帧分析降低 95% 以上；
- 缺陷检测 → 工单闭环传统需 **3–5 天**，本系统可压缩至 **数小时**；
- 全程数据留痕，支持 Word 报告导出，满足管理与审计需求。

---

## 二、系统核心功能详解

### 2.1 图片检测

**功能描述**：上传单张或多张巡检照片，系统自动识别以下 **8 类目标**：

| ID | 类别 | 分类 |
|----|------|------|
| 0 | 鸟巢 | 异物 |
| 1 | 风筝 | 异物 |
| 2 | 气球 | 异物 |
| 3 | 垃圾 | 异物 |
| 4 | 绝缘体外壳 | 正常 |
| 5 | **破损绝缘壳** | **缺陷** |
| 6 | **闪燃损坏绝缘壳** | **缺陷** |
| 7 | 良好绝缘壳 | 正常 |

**核心能力**：
- **YOLO 实时推理**：标注检测框、类别名称与置信度；
- **EXIF 自动提取**：自动读取照片 GPS 坐标与拍摄时间，无需手工录入；
- **批量上传**：一次上传多张图片，以网格形式展示每张检测结果，支持逐张查看详情；
- **AI 语义分析**：开启后对缺陷目标输出严重等级、成因与维修建议；
- **兜底异物检测**：独立扫描 YOLO 未覆盖的未知异物（如塑料袋、山火、工程机械），黄色标签警示。

### 2.2 视频分析

**功能描述**：上传巡检视频，系统执行跳帧 + 批量推理 + 硬件编码，并生成整段视频的 AI 总结报告与带标注框的 MP4 视频文件。

**核心能力**：

| 能力 | 说明 |
|------|------|
| **智能跳帧** | 场景变化检测，画面静止时复用推理结果，分析时间缩短 40–60% |
| **YOLO 批量推理** | 多帧一次性推理，GPU 利用率提升 2–3 倍 |
| **整段视频 AI 总结** | 全程**只调用 1 次 AI**，生成结构化总结（总体描述 / 主要问题 / 风险等级 / 建议 / 重点关注） |
| **NVENC 硬件编码** | 标注视频用 GPU 硬件编码合成，速度提升 5–10 倍（无 ffmpeg 自动降级） |
| **进度可视化** | 实时进度条 + 预计剩余时间 |
| **MD5 缓存** | 相同视频重复上传**秒级返回**历史结果，不重复调用 AI |
| **帧缩略图网格** | 所有帧保存为图片，网格展示，点击查看单帧详情 |
| **单帧等级修改** | 任意帧可手动改预警等级（正常 / 一般 / 严重 / 紧急）→ 直接生成工单 |
| **标注视频下载** | 输出 H.264 MP4，标注框实时绘制，可下载 |
| **默认处理时长** | 前 60 秒（可通过 `MAX_VIDEO_SECONDS` 环境变量调整） |

**"启用 AI 分析"开关**：勾选时视频分析完成后调用 1 次 AI 生成整段总结并显示；不勾选则仅做 YOLO 检测，不调用任何 AI API。

### 2.3 实时摄像头检测

**功能描述**：调用电脑摄像头，对现场画面进行实时 YOLO 检测。

**核心能力**：
- **准实时检测**：约 3–5 FPS 实时推理；
- **Canvas 流光框**：检测框带发光渐变特效，缺陷高亮醒目；
- **实时滚动日志**：检测结果在页面右侧实时滚动显示，支持开启/关闭语音播报。

> 摄像头模式不调用 AI 大模型，仅做 YOLO 推理。

### 2.4 多模态 AI 分析（功能开关）

**功能名称**：启用 AI 分析

**作用**：
- **图片检测**：对 YOLO 检测到的缺陷目标（类别 5/6 且置信度 > 0.7）调用 Qwen3-VL-Flash 进行深度语义分析，输出自然语言诊断报告（严重等级 / 成因 / 维修建议 / 是否预警）。
- **视频分析**：处理完成后调用 1 次 AI，对**整段视频**生成综合总结（取代早期逐帧分析，成本与耗时大幅降低）。

| 对比维度 | 开启 AI 分析 | 关闭 AI 分析 |
|----------|--------------|--------------|
| **图片：缺陷语义描述** | ✅ 如"图片右侧绝缘子第二片存在约 2cm 裂痕" | ❌ 无 |
| **视频：整段总结** | ✅ 总体描述 + 风险等级 + 建议 | ❌ 仅 YOLO 帧检测 |
| **严重程度分级** | ✅ 一般 / 严重 / 紧急 | ❌ 无 |
| **故障成因推断 / 维修建议** | ✅ | ❌ |
| **智能预警 + 语音播报** | ✅ 严重/紧急时触发 | ❌ 仅 YOLO 置信度 |
| **API 调用成本** | 图片逐张 + 视频仅 1 次 | 0 |

> **建议**：推荐开启，以获得完整诊断信息；视频分析开启后成本依然极低（每视频仅 1 次调用）。

### 2.5 兜底异物检测（功能开关）

**功能名称**：启用兜底异物检测

**作用**：YOLO 仅能识别训练数据中的 8 类目标，实际巡检中可能出现**未知异物**（如塑料袋、工程机械、山火、烟雾等）导致漏检。本功能调用 Qwen3-VL-Flash 对图片进行独立语义扫描，作为"第二道防线"。

| 对比维度 | 开启兜底检测 | 关闭兜底检测 |
|----------|--------------|--------------|
| **覆盖范围** | YOLO 8 类 + 任意未知异物 | 仅 YOLO 8 类 |
| 异物标记 | ✅ 黄色"潜在异常"标签警示人工复核 | ❌ 漏检异物被忽略 |
| 对工单流程的影响 | 不强制生成工单，由人工决定 | 不生成工单 |

> **建议**：推荐开启，防止遗漏未知风险。

### 2.6 工单闭环管理

**功能描述**：从"发现缺陷"到"修复完成"的全流程数字化管理，每个环节留痕可追溯。

**核心流程**：

```
发现缺陷 → 创建工单 → 指派 Worker → 接单处理 → 提交复检 → 审核闭环
  (Admin)    (Admin)      (Admin)      (Worker)   (Worker)    (Admin)
```

**角色分工**：

| 角色 | 职责 |
|------|------|
| **Admin（运维人员）** | 执行检测、创建/派发工单、审核复检、闭环确认、数据查看 |
| **Worker（检修人员）** | 接收工单、现场处理、提交复检（照片 + 备注） |

**关键特性**：
- **6 状态流转**：待派发 → 处理中 → 待复检 → 已闭环 / 已驳回，支持驳回后重新派发；
- **派发即带全信息**：派发工单界面展示缺陷图 + **AI 分析摘要**（图片的 AI 报告 / 视频的整段总结），Worker 端工单详情同样完整展示图片与 AI 摘要；
- **GPS 小地图点选**：派发工单界面内置小地图，点击即可自动填充经纬度，无需手工录入；
- **并排对比审核**：复检审核时原始缺陷图与修复照片**左右并排**展示，一目了然；
- **操作日志时间线**：创建、接单、复检、闭环等每一步自动记录操作人与时间；
- **闭环报告导出**：工单闭环后一键导出含文字 + 图片的 Word 报告，用于存档与归档。

**单帧预警等级手动修改**：在视频帧详情中，运维可将任意帧（无论 YOLO 判定为正常还是缺陷）的等级手动改为 正常 / 一般 / 严重 / 紧急，修改后即可生成工单（使用修改后的等级），且修改记录（修改人 / 时间）持久化可追溯。

### 2.7 数据仪表盘

**功能描述**：可视化展示系统运行数据，辅助管理决策。

**核心图表**：
- **缺陷检出趋势图**：近 7 / 30 / 90 天缺陷检出数量变化；
- **缺陷类别占比环形图**：各类缺陷占比一目了然；
- **工单状态统计卡片**：待派发 / 处理中 / 待复检 / 已闭环实时统计；
- **数据隔离**：Worker 仅能看到与自己相关的工单数据。

### 2.8 GIS 地图总览

**功能描述**：将带 GPS 坐标的检测结果与**未闭环工单**标注在电子地图上，实现"缺陷位置一张图"。

**核心能力**：
- **检测记录标记**：彩色圆点按严重程度分级（一般绿 / 严重橙 / 紧急红）；
- **未闭环工单位置**：📌 图钉标记所有尚未闭环的工单（待派发 / 处理中 / 待复检 / 已驳回），弹窗显示工单标题、状态、严重度、检修人、缺陷图；已闭环工单自动隐藏；
- **标记点弹窗**：点击标记查看图片缩略图与缺陷信息；
- **权限隔离**：Admin 查看全部标记，Worker 仅见自己负责的工单与记录；
- 支持手动录入 / 修改 GPS 坐标（针对无 EXIF 信息的照片）。

### 2.9 移动端访问优化

**功能描述**：针对手机等移动设备优化，公网环境下仍保持流畅体验。

**核心能力**：
- **图片上传前压缩**：移动端上传图片自动压缩（长边 1024px，JPEG 质量 0.7），公网传输缩短 60–80%；
- **视频上传前压缩**：移动端大视频（≥50MB）上传前用 ffmpeg.wasm 压缩至 720P，上传时间缩短约 70%；
- **检测结果精简返回**：移动端自动返回缩略图，避免加载大图；
- **列表分页 / 轮询适配**：移动端每页条数减少、视频进度轮询间隔放宽，切后台自动暂停。

---

## 三、完整演示案例（评委讲解脚本）

**场景设定**：某 220kV 输电线路运维人员使用本系统进行日常巡检，发现疑似绝缘子破损缺陷。

### 场景 A：图片巡检 → 派发 → 闭环（约 5–8 分钟）

| 步骤 | 操作 | 参与角色 | 演示要点 |
|------|------|----------|----------|
| **1. 登录系统** | 使用预置账号 `admin/123456`（Admin 运维）登录 | Admin | 角色权限：Admin 进入检测中心 |
| **2. 图片检测** | 上传一张"破损绝缘子"巡检照片，勾选"启用 AI 分析" | Admin | YOLO 实时标注缺陷框 → AI 输出"严重"等级 + 成因 + 维修建议 |
| **3. 派发工单** | 点击"派发"→ 选择 Worker → 确认派发（含 AI 摘要 + GPS 可地图点选） | Admin | 从"发现"到"派发"极速完成 |
| **4. 角色切换** | 无痕窗口 `worker/123456` 登录 | Worker | 角色数据隔离 |
| **5. 接收工单** | 进入"工单管理"，查看指派给自己的工单 | Worker | 查看缺陷图片 + **AI 分析摘要** + 操作日志时间线 |
| **6. 处理与复检** | 现场处理后"提交复检"→ 上传修复照片 + 处理备注 | Worker | 工单进入"待复检"状态 |
| **7. 审核闭环** | 切回 Admin → 打开工单详情，**左右对比**原始图 vs 修复图 → "同意闭环" | Admin | 并排对比审核 + 闭环备注留痕 |
| **8. 导出报告** | 工单"已闭环"后点击"📄 导出报告"→ 下载 Word | Admin | 报告含基本信息表 / 对比图 / AI 分析 / 日志时间线 |

### 场景 B：视频分析 → 整段总结 → 单帧改级 → 地图查看（Phase 26-30 亮点）

| 步骤 | 操作 | 参与角色 | 演示要点 |
|------|------|----------|----------|
| **1. 上传视频** | 视频标签页上传巡检视频，勾选"启用 AI 分析" | Admin | 实时进度条 + **预计剩余时间** |
| **2. 视频分析完成** | 分析完成后查看"🤖 AI 分析总结（整段视频）" | Admin | **全程仅调用 1 次 AI**，输出总体描述 / 风险等级 / 建议 |
| **3. 查看帧网格** | 打开视频详情，点击某一"正常"帧缩略图 | Admin | 帧缩略图网格 + 单帧详情 |
| **4. 修改预警等级** | 点击"✏️ 修改等级"→ 将该帧改为"严重"→ 确认 | Admin | 任意帧可改等级，修改记录（修改人/时间）留痕 |
| **5. 生成工单** | 等级修改后点击"📤 生成工单"→ 派发（自动带 AI 摘要 + 当前帧图片） | Admin | 修改等级后"正常"帧也能生成工单 |
| **6. 地图查看** | 打开"地图总览"，查看未闭环工单的 📌 标记 | Admin | **未闭环工单在地图上直观展示位置**，已闭环自动隐藏 |
| **7. 重复上传缓存** | 再次上传同一视频 | Admin | **MD5 缓存**命中，秒级返回结果，不重复调用 AI |

**案例价值总结**：
- **演示全程**：约 **6–10 分钟**；"从发现到派发"仅需 **30 秒**；
- **效率提升**：传统流程缺陷闭环需 **3–5 天**，本系统在 **数小时内** 即可完成；
- **成本降低**：视频 AI 分析从"逐帧调用 N 次"优化为"整段 1 次"，成本降低 95% 以上；
- **全程留痕**：每个环节有操作人、时间、图片、备注，满足电力行业管理与审计要求。

---

## 四、系统架构简图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         用户浏览器（Web 前端）                        │
│        图片 / 视频 / 摄像头 → 上传 → 结果 / 地图 / 仪表盘 / 工单       │
└───────────────────────────────┬─────────────────────────────────────┘
                                │ RESTful API
┌───────────────────────────────▼─────────────────────────────────────┐
│                        FastAPI 应用层                                │
│     认证鉴权（Admin/Worker 双角色） → Service 层 → 数据持久化         │
└───────┬──────────────────┬──────────────────┬────────────────────────┘
        │                  │                  │
        ▼                  ▼                  ▼
┌──────────────┐   ┌───────────────┐   ┌──────────────┐
│  YOLO 检测器 │   │ Qwen3-VL-Flash │   │  SQLite/MySQL │
│ 8类目标识别   │   │ 多模态大模型    │   │ 记录/工单/日志 │
│ 跳帧+批量推理 │   │ 视频整段总结    │   │ 全文留痕       │
│ GPU 串行推理  │   │ 图片逐张分析    │   │ video_summary │
└──────────────┘   └───────────────┘   └──────────────┘
        │                  │                  │
        └──────────────────┴──────────────────┘
                           │
                           ▼
      ┌───────────────────────────────────────────┐
      │  视频合成：ffmpeg NVENC 硬件编码（可选）     │
      │  移动端压缩：ffmpeg.wasm（≥50MB 视频）      │
      └───────────────────────────────────────────┘
                           │
                           ▼
              ┌─────────────────────────┐
              │  工单闭环管理（6状态流转） │
              │  创建→处理→复检→闭环/驳回  │
              └─────────────────────────┘
                           │
                           ▼
              ┌─────────────────────────┐
              │  GIS 地图：检测记录 +     │
              │  未闭环工单位置（📌）      │
              └─────────────────────────┘
```

**核心链路**：`数据输入 → YOLO 检测（跳帧+批量） → AI 分析（图片逐张 / 视频整段） → 视频合成（NVENC） → 工单闭环 → 地图可视化 → 报告归档`

---

## 五、常见问题 FAQ

**Q：系统支持哪些图片格式？**
A：JPG / PNG / BMP / TIFF / WebP。

**Q：支持哪些视频格式？视频分析最长多久？**
A：支持 MP4 / AVI / MOV / MKV / WMV / FLV / WebM。默认分析前 60 秒，可通过 `MAX_VIDEO_SECONDS` 环境变量调整。

**Q：视频分析为什么快？**
A：三项核心优化——智能跳帧（静止画面跳过推理）、YOLO 批量推理（GPU 利用率提升）、NVENC 硬件编码（合成提速 5-10 倍）。

**Q：视频 AI 分析为什么只调用 1 次 API？**
A：系统将早期"逐帧调用 AI"优化为"整段视频一次性总结"：处理完成后选取覆盖不同缺陷类型的代表性帧 + 缺陷统计，一次调用 Qwen3-VL 生成结构化总结。成本与耗时降低 95% 以上。

**Q：同一视频重复上传会重复分析吗？**
A：不会。系统按文件 MD5 缓存结果，相同视频再次上传**秒级返回**历史分析结果，不重复调用 AI。

**Q：AI 分析是否需要联网？**
A：需要。AI 分析与兜底检测调用 Qwen3-VL-Flash API，需在 `.env` 中配置 `DASHSCOPE_API_KEY`。YOLO 检测可完全离线运行。

**Q：标注视频合成失败 / 输出为空？**
A：系统优先使用 ffmpeg `h264_nvenc` 硬件编码（需 ffmpeg 且 NVIDIA 驱动满足编码器要求，最新 ffmpeg 构建可能要求驱动 ≥610）。不支持时自动降级 libx264 / OpenCV，不会中断分析。

**Q：普通帧能生成工单吗？**
A：可以。在视频帧详情中可将任意帧的预警等级手动修改为 严重 / 紧急 等，修改后即可生成工单，且修改记录持久化可追溯。

**Q：数据存储在哪里？**
A：默认 SQLite（`database/power_inspection.db`，零配置），可通过 `.env` 切换至 MySQL。

**Q：没有 GPS 信息的照片如何处理？**
A：系统支持在历史记录中手动录入 GPS 坐标，录入后即在地图中展示。

**Q：地图上为什么看不到已闭环工单？**
A：地图只显示**未闭环**工单（待派发 / 处理中 / 待复检 / 已驳回），工单闭环后自动从地图隐藏，突出待处理事项。

**Q：检测结果如何归档？**
A：图片检测记录与视频检测记录统一在"历史检测记录"中管理，支持 Word 报告导出、工单派发与删除。

**Q：如何保证多人协作时的数据安全？**
A：系统内置 Admin（运维）/ Worker（检修）双角色权限隔离，Worker 仅能查看指派给自己的工单与相关数据。

---

*本手册由电力输电线路智能检测分析预警系统项目组编制，对应系统 v3.0（Phase 0–30）。*
